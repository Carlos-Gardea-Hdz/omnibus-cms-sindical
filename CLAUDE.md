# CLAUDE.md — Corporate CMS (omnibus-cms)

> **Pointer file.** Per the agents.md standard, the canonical agent rulebook for
> this repo is **[`AGENTS.md`](./AGENTS.md)** — read it first. This file exists so
> Claude Code picks up repo context, and only points; it deliberately holds no
> rules of its own, to avoid drifting out of sync with the real sources.

## Read these, in order

1. **[`AGENTS.md`](./AGENTS.md)** — canonical agent rulebook + the honest current
   state of the code (it is a scaffold + landing page, not yet a CMS).
2. **[`SPEC.md`](./SPEC.md)** — Single Source of Truth for the product (domains,
   data model, routes, security, demo mode, phased plan). Code conforms to SPEC.
3. **[`NEXT_STEPS.md`](./NEXT_STEPS.md)** — verified gate results, known gaps, and
   the checklisted gap-to-done for chapter 3.1–3.3 + what merge to `main` requires.
4. The personal OMNIBUS vault (`~/.claude/CLAUDE.md` + the `~/dotfiles` AGENTS vault)
   — the global engineering law that governs every OMNIBUS repo.

> ⚠️ Do not re-add the package list, PHP version, or "gates pass" claims here.
> Those live in `AGENTS.md`/`SPEC.md` with the current drift called out honestly.
> Duplicating them is exactly how `CLAUDE.md` drifted before.
